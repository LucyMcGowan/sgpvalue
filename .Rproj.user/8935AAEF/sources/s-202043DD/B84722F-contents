## Compute Second generation pv

pdelta=function(ci.up,ci.lo,hi,lo){
	interval=ci.up-ci.lo
	null=hi-lo
	
	intersect=min(hi,max(ci.up,lo))-max(lo,min(ci.lo,hi))
	
	correct=max(0.5*interval/null,1)
	value=intersect/interval*correct
	
	return(value)
}
